#!/bin/sh -e
if [ "$1" = "configure" ]; then
  which vboxmanage || dpkg -i /opt/nanobox/share/virtualbox.deb
  which vagrant    || dpkg -i /opt/nanobox/share/vagrant.deb
  which nanobox    || dpkg -i /opt/nanobox/share/nanobox.deb
fi
